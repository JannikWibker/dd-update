options:
  use: web
  web: https://ifconfig.me/ip
  cache: true
  verbose: false

domain.name:
  daemon: 3600
  protocol: cloudflare
  server: api.cloudflare.com/client/v4
  ssl: yes
  login: email/username
  password: password
  zone: domain.name